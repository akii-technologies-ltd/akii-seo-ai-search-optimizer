# Assets

Submission-ready assets for the Claude Plugin Directory.

## screenshots/
3–5 PNG screenshots, each ≥1000px wide, response-only crops (no user prompt visible).

Suggested set:
1. `01-audit-report.png` — full audit scorecard + per-engine breakdown
2. `02-visibility-score.png` — composite + 4-vector table
3. `03-engine-profile.png` — per-engine vulnerability map
4. `04-schema-generated.png` — JSON-LD output + validation
5. `05-monitor-diff.png` — week-over-week visibility delta

Capture by running each slash command in a real session, then cropping the rendered output. Use a clean terminal theme (dark or light, consistent across all 5).

## icon/
Optional. If supplied: 512x512 PNG, transparent background, square.
